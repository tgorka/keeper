// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { RoomVm } from "./RoomVm";

/**
 * One index-based room-list operation mirroring an eyeball-im `VectorDiff`
 * (AD-8, AD-20).
 *
 * The SDK's `entries_with_dynamic_adapters` stream is recency-sorted; keeper
 * forwards its `VectorDiff` sequence verbatim as these ops. The frontend
 * applies them to a plain array by index and **never** re-sorts. Serialized as
 * an internally tagged enum so the frontend can switch on `op`.
 */
export type RoomListOp = { "op": "reset", 
/**
 * The complete current window, in order.
 */
rooms: Array<RoomVm>, } | { "op": "append", 
/**
 * Rooms to append.
 */
rooms: Array<RoomVm>, } | { "op": "clear" } | { "op": "pushFront", 
/**
 * The room to prepend.
 */
room: RoomVm, } | { "op": "pushBack", 
/**
 * The room to append.
 */
room: RoomVm, } | { "op": "popFront" } | { "op": "popBack" } | { "op": "insert", 
/**
 * The insertion index.
 */
index: number, 
/**
 * The room to insert.
 */
room: RoomVm, } | { "op": "set", 
/**
 * The index to overwrite.
 */
index: number, 
/**
 * The replacement room.
 */
room: RoomVm, } | { "op": "remove", 
/**
 * The index to remove.
 */
index: number, } | { "op": "truncate", 
/**
 * The new length.
 */
length: number, };
