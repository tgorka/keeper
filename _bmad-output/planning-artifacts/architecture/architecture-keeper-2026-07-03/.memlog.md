---
scope: keeper MVP system architecture (macOS, Tauri 2 + Rust matrix core + React renderer)
purpose: build-substrate
altitude: initiative
updated: 2026-07-09T16:30
---

- (constraint) ADOPTED (brief addendum, product-inputs, repo): matrix-rust-sdk 0.18 embedded directly in Tauri Rust backend; ALL Matrix state/crypto/persistence in Rust; React renders view models only; SSS (MSC4186) only; multi-account = N Client instances with per-account sqlite stores; IPC = commands + tauri ipc Channel VectorDiff streams + events for low-frequency; keeper-media custom protocol for decrypted media; client-only; Apache-2.0 with cargo-deny GPL/AGPL firewall; secrets in macOS Keychain
- (constraint) ADOPTED from repo reality: package manager is bun (bun.lock, project-context.md), not pnpm as research suggested; single tauri crate keeper/keeper_lib currently exists in src-tauri; quality gates biome2/clippy -D warnings/vitest4/nextest/lefthook/CI already wired; unsafe_code deny; unwrap_used warn
- (version) Verified 2026-07-03/07: matrix-sdk & matrix-sdk-ui 0.18.0; tauri 2.11.x; ts-rs 12.0.1 stable (crates.io); tauri-specta still 2.0.0-rc.24 (not stable); zustand 5.0.14; keyring crate ecosystem current (keyring-core + apple-native-keyring-store); React 19.1, TS 5.8, Vite 7, Tailwind 4, Biome 2.5 in repo
- (decision) AD paradigm: hexagonal (ports and adapters) Rust core + unidirectional view-model projection. keeper-core owns domain + matrix orchestration behind ports; tauri shell is an adapter; React is a pure renderer of streamed VMs; all mutations round-trip through Rust commands. Prevents: two sources of truth, matrix logic leaking to TS
- (decision) AD crate layout: convert src-tauri to cargo workspace: keeper-core (NO tauri dependency; modules: accounts, auth, sync, inbox, timeline, send, drafts, archive, bridges, signals(incognito), notify, keychain, settings, vm(IPC DTOs)) and keeper (tauri shell: commands, channels, keeper-media protocol, plugins, tray). Dependency direction: keeper -> keeper-core -> matrix-sdk; keeper-core never imports tauri. Prevents: core tauri-bound, blocks mobile reuse and pure-Rust testing
- (decision) AD type sharing: ts-rs 12 derives on every IPC DTO (serde-compat), exported to src/lib/ipc/gen via cargo test export; hand-written thin typed wrappers (invoke + Channel) in src/lib/ipc. Rejected tauri-specta: still release-candidate; stacking an RC binding framework on matrix-sdk 0.x churn is double churn; ts-rs is stable, zero runtime coupling. CI diff-check keeps bindings fresh
- (decision) AD frontend state: zustand 5 vanilla stores created outside React, one per stream domain (accounts, inbox, timeline per open room, bridges, send/outbox, drafts/approval, settings); channel handlers apply VectorDiff batches imperatively off-render; components subscribe via selectors. Rejected TanStack Query (pull/cache model mismatches push streams) and bare useReducer (state must outlive components). Stores are mirrors only; no derived truth in JS
- (decision) AD IPC contract: commands named domain_verb snake_case; every fallible command returns Result<T, IpcError{code stable enum, message, account_id?, retriable bool}>; subscriptions are commands taking Channel<Batch> and returning subscription id; every stream opens with a reset/snapshot batch then VectorDiff ops so resubscribe is always safe; events (low-frequency) named keeper://kebab-case. JSON payloads; media never over IPC
- (decision) AD storage layout: app data dir = accounts/<account_id>/sdk/ (matrix-sdk-sqlite state+crypto+event-cache+media cache; deleted on logout), keeper.db (app DB: drafts, outbox, settings, bridge registry, account registry), archive.db (Local Archive: normalized events for ALL accounts keyed by account_id + FTS5; never touched by logout; per-account delete = explicit DELETE). One archive DB, not per-account files: SQLite ATTACH limit (default 10) breaks unlimited accounts for cross-account FTS. Keychain via keyring crate (apple-native store): access/refresh tokens, recovery keys, store passphrases; service dev.tgorka.keeper
- (decision) AD archive ingestion: archiver task per account subscribes to decrypted sync/timeline events post-decryption in Rust and appends normalized rows (event_id, account_id, room_id, sender, origin_ts, type, content_json, edit-version chain, redaction flag, media metadata). Edits append versions, redactions mark not erase (honor-remote-deletions setting governs retention only; timeline VIEW always honors redaction). Single serialized writer per DB, WAL mode (NFR-8). Export JSON/Markdown reads archive.db only, background task with progress
- (decision) AD FTS: SQLite FTS5 with trigram tokenizer (case-insensitive) — handles CJK without word segmentation and accelerates LIKE/GLOB for sub-3-char queries; external-content table over archive messages, incremental index on ingest. Closes PRD open question 6. 200ms/100k bar validated in CI perf test
- (decision) AD undo-send: approved sends INSERT into outbox (keeper.db) with dispatch_at = approval_time + window (0-60s, default 10); tokio scheduler moves elapsed rows into that account SendQueue; cancel during window deletes row and restores draft text to composer/drafts. Crash/offline: on restart elapsed rows dispatch immediately (PRD FR-46 assumption), unelapsed resume countdown. Held messages projected into timeline VM as held state with countdown by send module. Post-dispatch delete = matrix redaction. FR-41 enforcement: the ONLY path into SendQueue is send::submit(draft_or_text, trigger) where trigger in {ComposerSend, ApprovalPaneApprove}; no other public API dispatches
- (decision) AD incognito: signals module in keeper-core is the SOLE emitter of outbound read receipts, typing notices and presence; resolves effective policy chat > account > global; while on: receipts sent as m.read.private, typing dropped, presence withheld where applicable; manual release = signals::release_receipt(room) emitting public m.read at current position. Rule: no other module and no tauri command calls SDK receipt/typing/presence APIs directly. Per-network coupling caveats (e.g. WhatsApp) are static data shipped with risk tiers
- (decision) AD drafts: drafts table in keeper.db is source of truth (crash-safe, FR-38); mirrored debounced to per-room account data custom event type dev.keeper.draft via SDK (FR-39); conflict policy: local wins, remote surfaced for one-tap adoption. Approval Pane = cross-account query over drafts + pending outbox rows. Also write SDK Room::save_composer_draft for Element-family interop where cheap
- (decision) AD bridge manager: trait BridgeTransport with impls Provisioning (bridgev2 HTTP provisioning JSON state machine rendered as native login states) and BotDriver (programmatic bridge-bot command send/parse with timeouts; raw bot chat never hidden). Discovery pipeline merges (a) GET /_matrix/client/v3/thirdparty/protocols, (b) data-driven known-bot MXID probe registry, (c) scan of existing bot DMs/portal rooms. Health: bridgev2 bridge state events + bot ping fallback -> BridgeSession state machine (healthy/degraded/disconnected), 60s surfacing bar via notify. bbctl = optional Tauri sidecar (Apache-2.0 Go binary, bundled per-arch, launch-on-demand exec with parsed output). Network Risk Tiers + coupling caveats = versioned JSON data file in repo
- (decision) AD auth: trait AuthProvider with password, oidc (SDK OAuth + system browser + deep-link callback keeper://oauth/callback via tauri-plugin-deep-link), beeper (port of Apache-2.0 bbctl api/beeperapi flow: POST /user/login -> /user/login/email -> /user/login/response -> JWT -> org.matrix.login.jwt on matrix.beeper.com). Beeper HTTP isolated in one module with typed failure states (UI shows Beeper login unavailable); its failure can never affect non-Beeper accounts. SSS capability check gates account creation (FR-5), result logged per account
- (decision) AD notifications: notify engine in keeper-core consumes post-decryption events, applies mute/mention-only/DND rules (persisted in settings; mapped to Matrix push rules where representable, evaluated locally otherwise), posts via tauri-plugin-notification with preview toggle; click payload deep-links (account_id, room_id, event_id) -> shell focuses chat. Bridge health alerts ride same pipeline. No push infrastructure ever on desktop (NFR-11)
- (decision) AD concurrency: per-account supervision tree — AccountManager owns registry of AccountHandle{Client, SyncService tasks, archiver, signals, send scheduler} spawned on tokio; cross-account aggregators (inbox merge, palette index, approval pane, notify) consume per-account streams; UI-facing state exposed via watch/broadcast channels consumed by the shell layer. Rule: no global mutable singletons except the tauri-managed AppState handle; cross-task state only via owned tokio sync primitives
- (decision) AD inbox projection: unified inbox computed in Rust by merging N RoomListService VectorDiff streams, sorted by recency with pins hoisted, favorites sectioned, archive state and Space filter applied core-side; UI receives a windowed VM stream (visible range + buffer, total counts) via one channel; Command Palette and Quick-Switcher query a Rust in-memory chat/action index via command (100ms/10k bar). Prevents: mirroring 10k chats into JS and JS-side sorting divergence
- (decision) AD media: keeper-media://<account_id>/<media_key> custom protocol with HTTP Range support streams decrypted bytes from the SDK media cache (thumbnails via SDK thumbnail API); media blob retention configurable (default keep) independent of message-text durability. No media bytes in IPC JSON ever
- (decision) AD errors/observability: thiserror enums per core module -> CoreError -> mapped once at shell boundary into IpcError envelope; tracing spans per account (fields: account_id, module; never message plaintext or tokens in logs), tracing-subscriber EnvFilter + rolling file logs in app log dir; panic hook logs via tracing; zero telemetry, crash reporting opt-in only if ever added (NFR-11). Egress surface documented per release in docs/egress.md
- (decision) AD at-rest encryption posture: SDK stores use matrix-sdk-sqlite native passphrase (first-run choice, key stored in Keychain). archive.db/keeper.db passphrase encryption is NOT in MVP: FTS5 cannot index ciphertext and SQLCipher linkage conflicts with matrix-sdk-sqlite bundled libsqlite3-sys (single native lib linkage); MVP relies on FileVault for the archive, honestly stated in settings copy. NFR-10 needs a PRD amendment narrowing "local stores" to SDK stores for MVP with archive-at-rest as a v1.x spike (sqlcipher-as-single-sqlite or page-level app crypto)
- (decision) AD packaging: GitHub Actions macos arm64 (aarch64 first, universal later) with tauri-action: Developer ID signing + hardened runtime + notarization via App Store Connect API key in secrets; tauri-plugin-updater artifacts signed with updater key; cargo-deny + all quality gates as PR checks; release job produces dmg + updater bundle + egress diff note
- (decision) AD future platforms: keeper-core stays tauri-free and platform-path-free (dirs injected via a Platform port trait: data dir, keychain impl, notifier sink). Plan A iOS/Android = Tauri mobile reusing keeper-core + same IPC contract (validate walking-skeleton iOS build before UI investment; known caveats: global-shortcut and updater are desktop-only plugins, tray/menubar desktop-only, iOS needs NSE + push infra decisions out of MVP). Plan B fallback = UniFFI bindings over keeper-core (Element X pattern) with native shells. The IPC DTO layer (vm module) is the stable seam either way
- (decision) AD tauri plugin set: notification, deep-link, global-shortcut, updater, autostart, window-state, clipboard-manager, opener; single-instance only when Windows/Linux arrive. NOT using tauri store or sql plugins: all settings and app data live in keeper.db behind Rust settings module (one source of truth, IPC-exposed), avoiding a second JS-writable store
- (decision) AD homeserver recommendation for companion-stack docs (PRD OQ2): Synapse (>=1.114, SSS default-on) as the documented default; conduwuit/continuwuity noted as watch-list until SSS support is verified stable. Docs-only artifact, no code dependency
- (question) OPEN: pre-architecture spike (PRD OQ1) not evidenced in repo beyond compiling deps — first epic must include walking-skeleton proof: SSS sync + E2EE decrypt + one timeline channel + FTS insert/query on macOS release build
- (question) OPEN: hungryserv partial C-S API surface (PRD OQ3) — which of thirdparty/protocols, custom room account data (dev.keeper.draft), m.read.private, push rules work on matrix.beeper.com; test against a real Beeper account in the first Beeper epic; degrade per-feature with disclosure, never block core Matrix accounts
- (question) OPEN: NFR-3 memory budgets and NFR-10 at-rest default remain owner-sign-off items (PRD OQ7); architecture provides measurement hooks (CI perf harness) but thresholds are not release-gating until confirmed
- (assumption) ASSUMPTION: account_id = keeper-generated opaque ULID mapped 1:1 to (mxid, device) pair, used in paths, archive rows, VMs and Keychain entries — avoids leaking server-derived strings into filesystem paths and survives re-login with new device_id via explicit migration
- (event) 2026-07-09 update run: extend spine with iOS phase (PRD §13 FR-55..65, NFR-15..18; addendum §7; research-ios-2026-07-09.md authoritative incl. Appendix A). AD-1..25 frozen; new decisions numbered AD-26+.
- (constraint) ADOPTED (PRD §13 + iOS research): AD-24 Plan A confirmed — Tauri 2 mobile reusing keeper-core + same IPC contract; Plan B (UniFFI+SwiftUI) shelved with recorded revisit triggers (blank-webview bug class unfixable; NSE work begins). Distribution this phase = free Personal Team signing (7-day profiles, ~3 devices, blocked entitlements: push/APNs, App Groups, iCloud, associated domains); no TestFlight/App Store; walking-skeleton gate (SM-7) before phone-UX investment.
- (version) Verified via live-web research 2026-07-09 (research-ios doc): Tauri 2 iOS target stable since 2.0 (Oct 2024); Xcode 16.x/iOS 18 SDK toolchain baseline; plugin matrix — notification+deep-link work on iOS; global-shortcut/updater/autostart/window-state/tray desktop-only; wry maps register_uri_scheme_protocol to WKURLSchemeHandler on iOS (scheme stays native, unlike Android http remap); keyring v3 apple-native targets iOS keychain (keyring-core + apple-native-keyring-store line); matrix-sdk-sqlite bundled rusqlite builds for aarch64-apple-ios (Element X ships same stack); known bugs tracked: tauri#14371 blank webview on resume, matrix-rust-sdk#3935 stale-resume sync restart
- (decision) AD-26 one shell crate, cfg-gated platform seams: iOS target is the SAME crates/keeper shell crate built as staticlib via tauri ios (crate-type already includes staticlib; tauri::mobile_entry_point). No keeper-ios crate ever. Desktop-only surface (tray module + tray-icon feature, global-shortcut, autostart, updater, window-state, desktop deep-link registration) behind #[cfg(desktop)]/#[cfg(target_os)] + target-gated Cargo deps; iOS shell registers notification + deep-link (mobile scheme) + IPC + media protocol only. keeper-core stays platform-free (Platform port, AD-24); no cfg(target_os) inside keeper-core business logic. Prevents: forked shell crate drifting from desktop; desktop plugin symbols in the iOS binary; core sprouting platform ifdefs
- (decision) AD-27 platform capability flags — single Capabilities VM: one CapabilitiesVm in keeper-core::vm (serde+ts-rs like every DTO), served over the IPC handshake at startup; data-driven per platform (iOS/macOS today, Android/iPad later reuse mechanism). Governs: bbctl sidecar (absent iOS), global hotkeys, updater controls, tray/menubar+autostart. UI renders unsupported surfaces NOT AT ALL (no dead buttons); programmatic reach of sidecar path returns clean IpcError Unsupported (Platform::sidecar_path errors on iOS — iOS forbids child processes). Bridge management beyond bbctl (discovery, provisioning login, bot fallback, health, risk tiers, start-chat) STAYS fully functional on iOS. Prevents: UI and shell disagreeing on platform surface; per-feature ad-hoc platform sniffing in TS
- (decision) AD-28 media on iOS unchanged: keeper-media:// via WKURLSchemeHandler (wry native mapping); URL format identical to macOS — no frontend change, no convertMediaSrc helper until Android actually starts. Range 200/206/416 slicing stays in-memory from SDK cache but buffer CAPPED per NFR-16 (jetsam); WebKit task invalidation (stopped scheme tasks) tolerated by existing fire-and-forget responder. Disk-backed streaming of large media = deferred work. Prevents: iOS-specific media transport fork; AD-4 'no media over IPC' eroding on mobile
- (decision) AD-29 iOS secrets + store protection: sessions/secrets through the EXISTING keyring/apple-native Platform port targeting the iOS keychain — spike-first in walking skeleton; contained fallback = direct security-framework generic-password calls behind the SAME Platform port (no call-site changes). Accessibility kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly (sync loop works after first unlock; never iCloud-keychain-synced — a Matrix device identity must not be cloned). SQLite/DB dirs: NSFileProtectionCompleteUntilFirstUserAuthentication (NOT Complete — WAL access after lock) + isExcludedFromBackup (multi-GB re-syncable state). All account state stays under one Platform data_dir() root so a future App Group move (NSE era) is a path change not a migration. Prevents: keychain impl leaking past the port; backup bloat; file-protection class breaking suspended sync
- (decision) AD-30 mobile lifecycle — foreground-only sync, honest: on background gracefully pause SyncService (matrix-sdk-ui stop/offline-mode) so long-poll is not killed mid-flight; on foreground resume + immediate sync, render cached mirror instantly (AD-8 snapshot-then-diff makes stale UI free) with subtle connecting state + stale-resume sync-loop restart guard (matrix-rust-sdk#3935). Detection: webview visibilitychange -> lifecycle command first (zero-native stopgap); micro Swift plugin on UIApplication notifications as the correctness upgrade path — same Rust entry point either way. NO background sync/push without APNs = paid-program decision gate, disclosed plainly in UI/docs (extends FR-53 honesty; FR-61). Blank-webview-on-resume reload guard (tauri#14371) mandatory from walking skeleton (NFR-18). Foreground local notifications + badge-on-sync only (FR-62), reusing AD-18 rules engine. Prevents: fake push promises; long-poll corpses; two lifecycle sources fighting over SyncService
- (decision) AD-31 phone layout tier — projection, not fork: third phone tier (<768px) added to existing useShellLayout (desktop/tablet tiers unchanged >=768px). Phone renders a stack-navigation container Inbox->Room->Detail REUSING existing InboxList/ChatView/DetailPanel components, driven by existing zustand selection state — the stack is a projection of selection state; NO router dependency this phase (history.pushState integration optional enhancer for system back gesture). Safe areas: viewport-fit=cover + contentInsetAdjustmentBehavior .never + env(safe-area-inset-*) as Tailwind theme CSS vars owned by app CSS. Keyboard: visualViewport-driven --kb-inset CSS var lifting composer (evaluate interactive-widget=resizes-content as simpler path); timeline pinned-at-bottom preserved. Touch idioms map to EXISTING actions (long-press = context menu, swipe rows = archive/mute, pull-to-refresh = kick sync). Prevents: forked chat components; a router creeping in as second navigation truth; per-component safe-area hacks
- (decision) AD-32 iOS build seed + CI gate: tauri ios init generates gen/apple under crates/keeper (workspace: relative to crate with tauri.conf.json); COMMIT gen/apple minus gitignored build/; persistent edits ONLY in project.yml (XcodeGen source of truth — .xcodeproj is regenerated/overwritten), Info.plist (CFBundleURLTypes keeper://), and *_iOS sources (safe-area Swift patch); min iOS 16.0 set explicitly. Signing: automatic Personal Team via bundle.iOS.developmentTeam or TAURI_APPLE_DEVELOPMENT_TEAM env (team ID out of git); bundle ID stable + shared with macOS (dev.tgorka.keeper). CI: cargo check --target aarch64-apple-ios as REQUIRED PR gate on existing macOS runner (compile-only, no signing); no simulator build in CI this phase. Prevents: edits lost to .xcodeproj regeneration; desktop PRs silently breaking the port; team IDs/signing state leaking into the repo
- (assumption) ASSUMPTION (carried from PRD §13, flagged there): NFR-15 3s cold-start bar is authored pending owner confirmation (not release-gating yet); badge counts total unreads across all accounts; iOS Local Archive slice is backup-excluded — desktop archive remains the durable exportable copy this phase (disclosed in docs)
- (question) OPEN (inherited from PRD §13.8, not architecture-blocking): NFR-15 cold-start number owner sign-off at phase release; paid-program timing (push/NSE/App Groups/TestFlight gate) — when push becomes a product goal it forces the PRD-level Sygnal/gateway question (client-only NFR-11: never project infrastructure)
- (event) reviewer gate (inline per headless hard rules): lint clean (0 findings); rubric walker PASS; currency lens PASS (all iOS claims trace to research-ios-2026-07-09.md live-web research); adversarial lens found 2 holes, both fixed at the gate — AD-30 badge pinned to inbox (AD-20) aggregate; AD-26 fixes web Clipboard API + native open call as iOS clipboard/opener replacements. Reviews in reviews/review-ios-update-*.md
- (event) spine finalized (iOS-phase update): AD-26..AD-32 added; AD-1..25 unchanged; binds extended to FR-1..65 / NFR-1..18; sources += research-ios-2026-07-09.md; updated 2026-07-09
