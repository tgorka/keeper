---
topic: keeper PRD — open-source Beeper-style Matrix client, macOS text-first MVP
updated: 2026-07-03T21:47
---

- (event) Headless create: PRD built from brief+addendum+product-inputs+2 research reports; no user questions; no subagents per caller hard rule — all extraction/review inline
- (decision) MVP scope locked per brief/caller: macOS text-first, 9 capability areas (accounts/auth, core messaging+E2EE, unified inbox+organization, bridge management, local archive+FTS+export, drafts+approval pane, incognito+undo-send, palette+keyboard, notifications); 54 FRs, 14 NFRs
- (decision) Beeper JWT login kept in MVP per brief scope (tech research suggested staging it to v1.x — brief overrides research)
- (decision) Bridge health split: 60s surfacing + re-login prompts = MVP (FR-28, top competitor complaint); aggregate health dashboard/alert center = v1.x — consistent with brief fast-follow list
- (decision) Spaces scoped to view/filter room-groups only (FR-23); creating/managing Spaces out of MVP — brief says 'Spaces as room-group views'
- (decision) FR-36: Local Archive preserves remotely edited/deleted content by default (core product promise), settings toggle to honor remote deletions; timeline view still honors redaction — rationale in PRD addendum §5
- (decision) FR-41 explicit-approval invariant elevated to product guardrail: exactly two user-initiated dispatch triggers; contract for future agent-proposal API
- (decision) SM-2 'flawless' gate scoped to Telegram/WhatsApp/Signal on self-hosted + Beeper account; other networks best-effort through same UX (falsifiable release gate)
- (assumption) Authored testable thresholds beyond brief: idle RAM 500MB/5acct + 300MB/1acct (NFR-3), chat-switch 150ms + 16ms input (NFR-4), notification 5s (NFR-7), at-rest encryption off by default (NFR-10) — all ASSUMPTION-tagged pending owner sign-off
- (assumption) Minor MVP trims, all ASSUMPTION-tagged: voice-note recording v1.x (playback in MVP), notification quick-reply v1.x, typing-only toggle bundled into incognito, per-network filter simple (custom views v1.x)
- (decision) Calls excluded from MVP per owner delegated decision (Element Call widget post-MVP) — deviation from owner required-feature #1 was decided upstream in brief, carried forward not re-decided
- (event) Input reconciliation (inline, all 5 inputs): 2 gaps found and fixed — Low-priority view added to v1.x list; owner's 'favorite contacts' mapped explicitly to Chat-based Favorites (FR-21)
- (event) Reviewer pass: inline rubric walk written to review-rubric.md; verdict strong across 7 dimensions; 2 fixes applied (SM-1 adopter count ASSUMPTION-tagged+indexed; FR-24 consequence restated testably)
- (override) Caller hard rule: no background/parallel subagents, no external reviewers — extraction, reconciliation, rubric review, and doc-standards structure+prose passes all performed inline by authoring session
- (decision) Open items triage: OQ1-7 deferred with owners (spike gates architecture sign-off, not PRD); 17 assumptions all carry defaults consistent with inputs; zero phase-blockers — JSON status reported complete with assumptions surfaced
- (event) PRD finalized
