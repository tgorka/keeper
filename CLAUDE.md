# keeper — Claude Code context

Read **[AGENTS.md](AGENTS.md)** — the architecture invariant, the Rust/TypeScript
rules, the quality gates, and the anti-patterns.

This file exists only so Claude Code finds the same rules under the name it looks
for, and is deliberately a pointer: both files are auto-loaded into every request,
so anything written twice is paid for twice.
